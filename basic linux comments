To create a new file :
  touch file_name

To print a text:
   echo good morning

To print date:
   date

To print host name:
   hostname

To print ip address:
   hostname -I

To create a new directory:
     mkdir directory_name
     cd directory_name

Move file inside directory:
     cp filename directory
     
Create another directory name:
     cd Desktop
     mkdir directory1

Copy one directory to another:
     cp directory directory1
     ls directory1

10.Remove first created directory:
     rmdir directory
     
 
	
